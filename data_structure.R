names <- c("Adi","Budi","Cindi","Dedi")

m <- matrix(1:12,nrow = 3)

a <- array(1:24, c(3,4,2))

users <- data.frame(
  name = names,
  gender = c("male","male","female","male"),
  age = c(10,20,30,40)
)